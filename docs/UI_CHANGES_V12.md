# UI changes v12

- Added `GlassComboBox` to remove the native focus artifact shown on the export quality selector.
- Single-option export fields are intentionally non-opening selectors.
- Added in-app asynchronous installation of DeepFilterNet and Silero VAD.
- Added live install progress and concise status feedback in the settings sheet.
- Added `csezet` creator tag to the bottom-left footer.
- DeepFilter executable discovery now checks the active Python environment's Scripts directory explicitly.
